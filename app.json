{
  "name": "Modmail Log Viewer",
  "description": "A simple webserver to view self-hosted logs",
  "repository": "https://github.com/chirusxs/logviewer",
  "env": {
    "MONGO_URI": {
        "description": "MongoDB connection URI that contains your modmail logs.",
        "required": true
        }
    }
}
